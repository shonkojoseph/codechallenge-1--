
//The function opt to accept numbers
//And generates an array between them
let arr = [1,2,3,4,5,67,8,9,0]
const primeNumbers = arr.filter(num => {
    for(let i = 2; i < num; 1++){
        while(num % i === 0)
        return num > 1;
    }
});

console.log(primeNumbers)